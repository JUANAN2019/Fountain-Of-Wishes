<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FountainWishes" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.FountainWishes" parent="Base.Theme.FountainWishes" />
    <style name="CornerSize">
        <item name="cornerSize">50%</item>
    </style>
    <style name="botones">
        <item name="cornerSize">50%</item>
    </style>
    <style name="Buttonspui" parent="Base.Widget.AppCompat.Button.Colored">
        <item name="colorButtonNormal">@color/purple_dark</item>
        <item name="colorPrimary">@color/purple_dark</item>
        <item name="colorControlHighlight">@color/white</item>
        <item name="android:textSize">20sp</item>
    </style>

</resources>